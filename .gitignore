tests/fixtures/**/node_modules/
*.tmp
.DS_Store

# Personal VS Code workspace file (user-specific; shared settings live in .vscode/)
defect-scan.code-workspace

# tooling caches / agent scratch
.ruff_cache/
.claude/agent-memory/

# defect-scan eval harness: transient per-run record (not ground truth)
tests/eval/*/.last-run.*

# internal-only (launch drafts, working notes) and local design/implementation
# history — kept on disk but never published to the public repo
docs/internal/
plans/
specs/
